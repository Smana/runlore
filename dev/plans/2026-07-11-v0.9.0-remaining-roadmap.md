# v0.9.0 — remaining roadmap (verified status snapshot, 2026-07-11)

Clean checkpoint. The lists below were **verified against the code** (file:line) by a read-only
recon pass. Validation plan: a **test cluster** run gates the v0.9.0 release.

## UPDATE 2026-07-11 (session 2): Wave 3 SHIPPED

Most of the backlog below is now **implemented and merged** on this branch (Waves 3A/3B/3C, all
file-partitioned parallel worktrees, full CI gate suite green each wave — build/vet/gofmt/`-race`/
golangci-lint 0 issues/govulncheck; `hack/demo.sh` + `lore demo investigate` smoke OK).

**Shipped this session:** M1 M2 M3 M4 (metrics onset-shape/clamp/topk/scalar) · L1 L2 L3 (log field
config + query_logs namespace confinement + fan-out sentinel/dedup) · K1 K2 (pod_status restarts+
times, kube_events window) · G1 (NoCheckout clone) · G2 (inspector event times+counts) · G3 (GitOps
Changes honor TimeWindow) · C1 (coalesced-batch constituents in seed) · C2 (verify+judge see a
bounded, redacted tool transcript) · C3 (rune-safe truncate) · N1 N2 N3 N4 (net sentinels, custom
awsvpc format, Karpenter+spot, Hubble TLS opt-in) · S1 S2 S4 S5 (Matrix defang, image-beacon
neutralize, digest-pinned images, Slack error logging) · P1 (**incident_timeline** tool) · P3
(resourcehealth honors window) · P4 (VM/VL backend-flavor detect + MetricsQL guidance).

**Also shipped since (session 2, later):**
- **G4 ownerReferences walk + drift detection** — new `workload_ownership` tool: walks
  Pod→controller→GitOps object (Flux/Argo tracking labels), reports a 3-tier drift verdict
  (Argo OutOfSync → Flux not-Ready-drift → last-applied-config diff). Live-spec-vs-Git-RENDER
  diff scoped out as future. Merged, green.
- **Q1 `Investigate` decomposition** — extracted `tryRecall`/`enforceBudget`; method 477→359
  lines; all 264 existing tests pass unchanged. Merged, green.

**Deliberately DEFERRED (do after the test-cluster gate) — with rationale:**
- **P5 remove legacy `network:{url}` shim** — BREAKING; confirm the test-cluster config uses
  `network.provider` FIRST. Maintainer decision.
- **P2 provider conformance suite + `internal/render` consolidation** — large cross-cutting refactor
  that rewrites every tool's output path; internal-quality ("quarter" item). Not worth destabilizing
  a release that's about to be validated. Recommended as its own focused PR.
- **stream_context (part of P4)** — VictoriaLogs stream_context is a stateful two-step flow; skipped
  as not-clean-enough. Optional follow-up.
- **eval transcript wiring** — C2 put the plumbing in `Judge.Grade`, but `eval/compare_run.go` +
  `live.go` don't pass a real transcript yet, so eval groundedness grading is dormant until wired.
- **S3 webhook-open-in-non-auto + permissive default NetworkPolicy**, **P6 dev/ docs** — policy/
  decision items, no safe unilateral change before the test-cluster run.

Original verified backlog map retained below for reference.

---

## Shipped in v0.9.0 (on branch `audit/2026-07-11-pre-announcement`)

Waves 1 & 2 — all confirmed audit defects (1 P0 + 13 P1) + verified P2s + doc backlog + the
approved differentiator features. Highlights: quickstart boots; silent alert-drops fixed +
`finish()` funnel; docs/code coherence; `Change.When` + Flux/Argo namespace resolution + bounded
diffs; log pod-attribution + first→last spans; multi-container `pod_logs`; network IP-scope
sentinel + pod IPs; CloudTrail single-attribute + `errorCode`; cluster-scoped ASG describe;
budget caps default in code; `discover_metrics`/`discover_log_fields`; `logs_error_summary`;
recall near-miss injection + enriched `kb_search`; reflection-walk egress redaction;
`controller_logs` Flux-gating; egress-client lint; `lore demo investigate`.
**Full CI gate suite green** (SPDX, build, vet, gofmt, `go test -race`, golangci-lint 0 issues,
govulncheck, demo). Details: `2026-07-11-v0.9.0-execution.md`.

## Remaining roadmap — verified, NOT yet implemented

Each item: files to touch · one-line sketch · test-cluster safety. Grouped for parallel work.

### Metrics (`query_tools.go` + `prometheus.go`)
- **M1** trend buckets + biggest-jump detector in `query_metrics_range` — `query_tools.go` `summarize` (:169-185) + render (:146-152). Additive. Safe.
- **M2** clamp `since_minutes`/`step_seconds`, derive step from window (Prometheus 11k-point limit) — `query_tools.go:128-138`, `prometheus.go:87-96`. Safe (annotate when clamping so an operator's explicit step isn't silently coarsened).
- **M3** sort instant series by |value| before the 50-cap; add aggregation-steering to `Description()` — `query_tools.go:80,55-57`. Safe.
- **M4** handle scalar/string PromQL result types (currently a raw unmarshal error) — `prometheus.go:52-84`. Safe.

### Logs (`query_tools.go` logs-half, `renderlog.go`, `victorialogs.go`, `config.go`, `investigate.go` logs-block)
- **L1** make collector field conventions configurable with safe defaults + document — `config.go`, `query_tools.go:254-278`, `renderlog.go:40-71`, `investigate.go:129-137`. **Safety: keep exact current strings as defaults — a wrong default breaks every logs query.**
- **L2** apply the `pod_logs` namespace allowlist to `query_logs` (same PII confinement) — `query_tools.go:294-325`, `investigate.go`, `loop.go` `scopeTools` (:361). **Safety MEDIUM: default to current permissive behavior unless allowlist set; always allow the incident namespace, or it blocks legit raw LogsQL.**
- **L3** sentinel when `pod_logs` fan-out cap (5) binds; optional numeric-normalized dedup — `cluster.go:24`, `renderlog.go:63-94`. Safe.

### Kube (`kube_tools.go`, `cluster.go`, `providers.go` KubeReader/PodStatus)
- **K1** add restart counts + timestamps (age/lastTerminated) to `pod_status` — `providers.go:344-357`, `cluster.go:161-216`, `kube_tools.go:71-80`. Additive. Safe.
- **K2** `kube_events` newest-first + `since_minutes` window param (Limit:200 currently grabs an arbitrary name-ordered page) — `providers.go:372-379` (additive param), `cluster.go:220-262`, `kube_tools.go:104-143`. Safe (add param, don't change arity).

### GitOps / whatchanged (`differ.go`, `flux/argocd`, `providers.go` inspector)
- **G1** `NoCheckout:true` on the clone (avoid full worktree checkout; ~1.3GB OOM noted) — `differ.go:170`. **Safety MEDIUM: verify diff paths read git objects, not the worktree, before shipping.**
- **G2** add timestamps + counts to GitOps inspector events (keep `Events []string`, just format) — `flux/dynamic.go:133-162`, `argocd/dynamic.go:78-105`. Safe.
- **G3/P3** honor `TimeWindow` in GitOps `Changes` (currently `_`) — `flux.go:94`, `argocd.go:83`; part of the TimeWindow contract. **Safety MEDIUM: changes what "what changed" surfaces.**
- **G4** ownerReferences walk (Pod→controller→Flux/Argo labels) + live-vs-Git drift detection — new `GitOpsInspector.OwnerChain`, new tool, `investigate.go` registration. Large (Phase-2-sized). Additive.

### Correlation / loop (`loop.go`, `verify.go`, `eval/judge.go`, `coalesce`)
- **C1** preserve coalesced-batch constituent workloads/namespaces in the seed — `coalescer.go:92-108`, `serve.go:200-214`, `loop.go` seedPrompt (:1049-1093). Safe.
- **C2** feed a redacted, size-capped tool transcript to the verify pass + eval judge — `verify.go`, `loop.go` (:306,661), `eval/judge.go`+`score.go`. **Safety MEDIUM: grows verify tokens + can shift verdicts; size-cap it.**
- **C3** rune-safe `truncateOutput` (currently splits multi-byte UTF-8) — `truncate.go:22-25`. Safe.

### Network / cloud (`awsvpc.go`, `hubble.go`, `resourcehealth.go`, `config.go`, `investigate.go` network-block)
- **N1** truncation sentinel when awsvpc/hubble caps bind (parity with gcpfirewall/cloudtrail) — `awsvpc.go:148-150`, `hubble.go:40-76`. Safe.
- **N2** configurable awsvpc flow-log field format (custom formats currently return "no dropped flows") — `awsvpc.go:57,164-193`, `config.go`, `investigate.go:152`. Safe (default unchanged).
- **N3** Karpenter + spot-interruption visibility in `cloud_resource_health` — `resourcehealth.go` (tag-based `DescribeInstances`). Additive, ~100-200 LOC.
- **N4** Hubble TLS option (currently always plaintext) — `hubble.go:33-42`, `config.go`, `investigate.go:145`. **Safety: default stays insecure/opt-in so the test cluster's Hubble still connects.**

### Security / supply (all fully disjoint, low risk)
- **S1** stop Matrix auto-linkifying untrusted URLs (Slack already neutralizes) — `notify/matrix.go:119-132`. Safe.
- **S2** neutralize markdown image beacons in GitHub-rendered bodies (KB/PR/issue) — `curator/draft.go`, `forge/github/github.go`. Safe.
- **S3** webhook fails open in non-auto modes + permissive default NetworkPolicy — `config.go` Validate, `networkpolicy.yaml`. **Safety HIGH: do NOT flip NetworkPolicy default-deny before the test-cluster webhook run; document instead.**
- **S4** digest-pin Dockerfile base images — `Dockerfile`, `Dockerfile.goreleaser`. Safe.
- **S5** log Slack `response_url` non-2xx/errors (currently swallowed) — `server.go:395-413`. Safe.

### Code quality
- **Q1** extract `tryRecall`/`enforceBudget` from `Investigate` (472 lines; `finish()` already landed) — `loop.go`. **Conflicts with all loop.go work — do first or last, alone.**
- **Q2** tests for `internal/app` cold spots (serve.go composition root) — new `_test.go`. Safe.

### Phase 2 (largest, cross-cutting)
- **P1** `incident_timeline` tool fusing GitOps + cloud changes + kube_events into one chronology — new `timeline_tool.go` + `investigate.go` registration. Additive.
- **P2** provider conformance suite (`internal/providers/providertest`) + output-shaping consolidation into `internal/render`. **Global barrier: rewrites query_tools.go + renderlog.go + kube_tools.go + truncate.go — run ALONE, before or after all M/L/K/C3.**
- **P3** TimeWindow as a real cross-provider contract (subsumes G3; + `resourcehealth.go`) — asserted in providertest.
- **P4** backend-flavor exploitation (auto-detect VM/VL → MetricsQL `outliersk`/`default 0`, TSDB cardinality, `stream_context`) — mostly tool descriptions.
- **P5** remove legacy `network:{url}` shim — `config.go:120-165`, `investigate.go:147-149`. **Safety BREAKING: confirm the test-cluster config uses `network.provider` first.**
- **P6** decide: keep `dev/**` planning docs as public design history vs. move out (no code impact).

## Tool registration wiring point
Single point: `BuildModelAndTools` in `internal/app/investigate.go:38-186`. Conditional by config/
capability (gp!=nil, `cfg.Metrics.URL`, `switch cfg.Network.Provider`, `GitopsEngine(cfg)=="flux"`
for controller_logs, etc.). Any NEW tool (incident_timeline, backend-flavor, drift) is appended here.

## Recommended resume plan (waves, disjoint file sets)
1. **Wave 3A (parallel, safe):** metrics {M1-M4} · security/supply {S1,S2,S4,S5} · isolated {C3,G1} · network sentinels/formats {N1,N2,N4} · kube {K1,K2}. Each owns a disjoint set.
2. **Wave 3B (parallel, after 3A):** logs {L1,L2,L3} (owns renderlog+victorialogs+logs-half of query_tools) · gitops {G2,G3} · cloud {N3} · correlation {C1,C2} (owns loop.go). Note: L2 touches loop.go — sequence after any loop.go work.
3. **Wave 3C (sequential, alone):** Q1 loop decomposition; then P2 render/providertest barrier; then P3 TimeWindow, P1 incident_timeline, P4 backend-flavor, G4 drift; finally P5 (breaking, after test-cluster config check).

## HARD parallelism rules (never run together — shared file)
- `loop.go`: Q1 ∥ C1 ∥ C2 ∥ L2 — Q1 alone; rest sequenced.
- `query_tools.go`: {M1-M4} ∥ {L1,L2} ∥ P2 ∥ P4 (metrics-half vs logs-half can split only if neither touches `renderRows`/`maxToolRows` at :16,35-43).
- `providers.go`: {K1,K2} ∥ G4 — serialize.
- `investigate.go` (app): logs-block ∥ network-block ∥ new-tool-append ∥ P5 — one wiring owner or sequence.
- `resourcehealth.go`: N3 ∥ P3 — serialize.
- `renderlog.go`/`kube_tools.go`: small — serialize their items.

## Method that worked (reuse it)
File-partitioned subagents, each in an isolated `git worktree` off this branch with an exclusive
file set → conflict-free merges. Controller runs the full CI gate suite at each integration.
Agents: TDD; SPDX line 1 on new .go files; Conventional Commits; NO co-author trailer; scoped
gates only (controller runs `-race` + golangci-lint v2.12.2 + govulncheck at integration).
