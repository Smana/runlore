---
title: Data sources
weight: 30
---

What the agent reads for signal. Every one is optional: an unset data source simply disables the
tool it would have unlocked, and RunLore runs without it. None is assumed and none is required.
