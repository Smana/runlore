---
title: LLM providers
weight: 20
---

What runs the investigation loop. Exactly one is configured at a time, under `model`. Any endpoint
that supports forced `tool_choice` will work — the loop depends on it.
